@model IEnumerable<int>

@foreach (var id in Model)
{
    @Html.RouteLink(id.ToString(), new
{
    controller = "Message",
    action = "MessageList",
    //currentDay = id,
    languageId = id
}, new
{
    @class = "btn btn-block btn-default btn-lg"
})
}
