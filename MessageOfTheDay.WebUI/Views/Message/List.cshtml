@model MessageOfTheDay.WebUI.Models.ContextModel
@{
    ViewBag.Title = "Message of the Day";
}

@foreach (var message in Model.Messages)
{
    <div>
        @message.TodaysMessage <br/>
        <img src="@Url.Content("~/Content/Images/" + Model.FlagPath)" />

    </div>
    //foreach (var language in Model.Languages.Where(language => language.Id == message.LanguageId))
    //{
    //    <div>
    //        @language.PartialFlagPath
    //    </div>
    //}
}
@Html.Partial("ShowDropdowns", new MessageOfTheDay.WebUI.Models.UserSelection(Model.Days, Model.Languages))